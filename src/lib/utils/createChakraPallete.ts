const createChakraPallete = (palxObject: Record<string, string[] | string>) => {
  console.log(Object.keys(palxObject));
  // const chakraPallete = Object.entries(palxObject).map((color) => {
  //   return palxObject[color].map();
  // });
};

export default createChakraPallete;
