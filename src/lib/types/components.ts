export type EventCardProps = {
  imgUrl: string;
  title: string;
  description: string;
  date: string;
  id: string | number;
};

export type LoginFormProps = {
  email: string;
  password: string;
};
